# See https://jitpack.io/docs/BUILDING/

jdk:
  - oraclejdk8
install:
   - echo "Running a custom install command to build Wemi"
   - java -jar wemi "core/publish; pluginJvmHotswap/publish"
env:
   WEMI_COLOR: "false"
